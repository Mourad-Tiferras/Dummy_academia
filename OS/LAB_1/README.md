### Q1: What output did you get?  
==> I get ``/usr/bin/zsh`` (I'm a bare-metal linux user)  

### Q2: What is the difference between ls and ls -la?   
==> ``ls`` list files and dir's in current directory, but ``ls -la`` show additional info including permessions, ownership, file size, and Date and hiddenfiles as well

### Q3:  What does cd ~ do?
==> It drives u to the home directory

### Q4: What's the difference between > and >>?  
==> That's Called shell redirecrion where ``>`` will ovverwrite the file or will creating a new one if not exist, and the ``>>`` will append to the file  
 exemple; ``echo "I'm Born2Code" > File.txt && echo "I'm Born2Hack" >> File.txt``  

### Q5: What does the -p flag do in mkdir -p?  
==> Simply that -p flag will create the directory if a parrent dir is missing without raising an error :)  

### Q6: How do you delete a directory with files in it?  
==> ``rm -rf``  

### Q7: How do you create and use a variable in Bash?    

==> Simply are created with ``=`` and u can access to it with a $ and curly braces, exemple; ``${my_var_name}``  

### Q8: Why do we need chmod +x?  
==> In order to give the exexution permession

### Q9: 
